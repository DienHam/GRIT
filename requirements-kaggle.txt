# Kaggle/Colab GPU requirements for GRIT scripts.
#
# Do not install the local Mac requirements.txt on Kaggle: keep the notebook's
# CUDA PyTorch build, then install/update only the HF/data utilities below.
transformers==4.53.3
datasets==4.8.5
accelerate==1.10.1
pandas==2.3.3
pyarrow>=19.0.0
tqdm
safetensors
sentencepiece
huggingface_hub>=0.30.0,<1.0
packaging>=20.0
openai>=1.0.0
